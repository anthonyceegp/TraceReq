<% content_for :title, "New" %>
<% navigation_add 'Projects', projects_path %>
<% navigation_add @project.name, project_path(@project) %>
<% navigation_add 'Artifact Types', project_artifact_types_path(@project) %>

<h2>New Artifact Type</h2>
<%= render 'form' %>